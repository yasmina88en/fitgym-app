<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="280dp"
    android:layout_height="wrap_content"
    card_view:cardCornerRadius="16dp"
    card_view:cardElevation="3dp"
    android:layout_marginEnd="16dp"
    android:layout_marginBottom="8dp"
    android:layout_marginTop="4dp"
    android:layout_marginStart="4dp"
    card_view:cardBackgroundColor="@android:color/white"
    android:foreground="?attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Image en haut (Cover) -->
        <ImageView
            android:id="@+id/ivSessionImage"
            android:layout_width="match_parent"
            android:layout_height="140dp"
            android:scaleType="centerCrop"
            android:src="@drawable/placeholder" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="12dp">

            <!-- Titre et Prix -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">
                
                <TextView
                    android:id="@+id/tvTitre"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Yoga Morning"
                    android:textStyle="bold"
                    android:textSize="16sp"
                    android:textColor="#1F2937"
                    android:maxLines="1"
                    android:ellipsize="end"/>

                <TextView
                    android:id="@+id/tvPrix"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="15€"
                    android:textStyle="bold"
                    android:textColor="#6D28D9"
                    android:background="@drawable/bg_chip_premium"
                    android:paddingHorizontal="8dp"
                    android:paddingVertical="2dp"
                    android:textSize="12sp"/>
            </LinearLayout>

            <!-- Date et Heure -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:layout_marginTop="6dp">

                <ImageView
                    android:layout_width="14dp"
                    android:layout_height="14dp"
                    android:src="@drawable/ic_calendar"
                    android:layout_marginEnd="4dp"
                    android:alpha="0.6"/>

                <TextView
                    android:id="@+id/tvDateHeure"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Lun. 12 Oct • 10:00"
                    android:textSize="13sp"
                    android:textColor="#6B7280"/>
            </LinearLayout>

            <!-- Tags (Niveau) -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginTop="8dp">

                <TextView
                    android:id="@+id/tvNiveauChip"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Débutant"
                    android:textSize="11sp"
                    android:textColor="#059669"
                    android:background="@drawable/bg_chip_green_light"
                    android:paddingHorizontal="8dp"
                    android:paddingVertical="4dp"/>
                
                <!-- On cache la catégorie car on est déjà sur la page du coach/catégorie souvent -->
                <TextView
                    android:id="@+id/tvCategorieChip"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:visibility="gone"/>
                    
                <TextView
                    android:id="@+id/tvWarning"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Complet"
                    android:textSize="11sp"
                    android:textColor="#DC2626"
                    android:layout_marginStart="8dp"
                    android:visibility="gone"/>
            </LinearLayout>
            
            <!-- Bouton Favoris caché ou discret -->
            <ImageButton
                android:id="@+id/btnFavorite"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:visibility="gone"/>
                
            <!-- Coach caché car on est sur sa page -->
            <TextView
                android:id="@+id/tvCoach"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:visibility="gone"/>

        </LinearLayout>
    </LinearLayout>
</androidx.cardview.widget.CardView>
